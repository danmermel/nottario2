{
  "name": "nottario-lambdas",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-lambda": "^3.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "pdf-lib": "^1.17.1",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "jest": "^29.0.0",
    "zip-a-folder": "^3.1.3"
  }
}
